services:
  - type: web
    name: rural-healthcare-clinic
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DB_HOST
        value: db
      - key: DB_USERNAME
        value: testuser
      - key: DB_PASSWORD
        value: secret
      - key: DB_NAME
        value: healthcare_db
